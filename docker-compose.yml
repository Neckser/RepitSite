services:
  app:
    build: .
    ports:
      - "127.0.0.1:8000:80"
    volumes:
      - app_data:/app/data
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1

volumes:
  app_data: